# secrets — never commit, only .env.example
.env

# runtime data (SqliteSaver checkpoints, etc.)
data/

# local MCP server config (machine-specific paths) — commit only the .example
mcp_servers.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# uv-managed virtualenv
.venv/

# test & lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# OS
.DS_Store
